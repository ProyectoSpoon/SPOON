// 🛑 DEPRECATED: ESTE ARCHIVO HA SIDO DESHABILITADO
// La arquitectura ha migrado completamente a Supabase SDK (HTTPS).
// Cualquier intento de importar 'pool' fallará intencionalmente.

/*
import { Pool, PoolConfig } from 'pg';
import dns from 'dns';

// ... (contenido comentado para referencia histórica si fuera necesario, o simplemente eliminado)
*/

export const pool = null as unknown as never; // ❌ ERROR DE COMPILACIÓN INTENCIONAL: Usar createRouteHandlerClient
export default pool;
